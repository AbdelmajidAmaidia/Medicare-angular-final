{
  "name": "medicare-ai-ui",
  "version": "0.0.0",
  "scripts": {
    "ng": "npx ng",
    "start": "npx ng serve",
    "build": "npx ng build",
    "watch": "npx ng build --watch --configuration development",
    "test": "npx ng test",
    "test:ci": "npx ng test --watch=false --code-coverage",
    "lint": "npx ng lint"
  }
}

